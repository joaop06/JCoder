import 'reflect-metadata';

// E2E test setup
beforeAll(async () => {
    // Setup for e2e tests
});

afterAll(async () => {
    // Cleanup after e2e tests
});

beforeEach(() => {
    // Setup before each e2e test
});

afterEach(() => {
    // Cleanup after each e2e test
});
